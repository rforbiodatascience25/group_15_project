---
title: "01_load"
format:
  html:
    embed-resources: true
editor: visual
execute: 
  echo: true
  message: false
  warning: false
---

### Load the data

```{r}
library(tidyverse)

# unzip the file to data folder
zip_file <- "../_raw/kidney_disease.zip"

# if ../data/ is not present in root, create it
if (!dir.exists("../data")) {
  dir.create("../data")
} 
else {
  message("Folder exists.")
}


# unzip the data into ../data
unzip(zip_file, exdir = "../data")

# read cvs
kidney_disease <- read_csv("../data/kidney_disease.csv")
```
